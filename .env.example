# --- Base de dados (PostgreSQL + PostGIS) ---
POSTGRES_USER=turismo
POSTGRES_PASSWORD=turismo
POSTGRES_DB=turismoconnect
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# URL completa usada pelo backend e pelo Alembic
DATABASE_URL=postgresql+psycopg2://turismo:turismo@localhost:5432/turismoconnect

# --- LLM (usado a partir da Sprint 02) ---
OPENAI_API_KEY=
GROQ_API_KEY=

# --- Backend ---
# Origens autorizadas pelo CORS (separadas por vírgula)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
